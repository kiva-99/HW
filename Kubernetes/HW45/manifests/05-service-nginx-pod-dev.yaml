apiVersion: v1
kind: Service
metadata:
  name: hw45-nginx-pod-service
  namespace: hw45-dev
  labels:
    app: hw45-nginx
    homework: hw45
spec:
  type: NodePort
  selector:
    app: hw45-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30082
