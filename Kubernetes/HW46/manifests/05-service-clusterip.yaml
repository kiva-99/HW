apiVersion: v1
kind: Service
metadata:
  name: hw46-nginx-clusterip
  namespace: hw46
spec:
  type: ClusterIP
  selector:
    app: hw46-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
