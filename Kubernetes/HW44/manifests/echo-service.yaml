apiVersion: v1
kind: Service
metadata:
  name: echo-service
  labels:
    app: echo-demo
spec:
  type: NodePort
  selector:
    app: echo-demo
  ports:
    - name: http
      protocol: TCP
      port: 5678
      targetPort: 5678
      nodePort: 30081
