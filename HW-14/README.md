# HW-14: Отладка Bash-скриптов

Скрипт демонстрирует методы отладки Bash:
- Передача аргументов (`$1`, `$2`)
- Попытка использовать несуществующую переменную (`$city`)
- Поддержка раскомментирования `set -x` или `trap` для отладки

## Запуск:
```bash
chmod +x debug-test.sh
./debug-test.sh Иван 30

q

q
